export interface IUserERC721Assets {
  lastUpdateBlockNumber: null | number;
  meta: any;
  needUpdate: boolean;
  ownerAddress: string;
  tokenAddress: string;
  tokenID: string;
  tokenURI: string;
}
